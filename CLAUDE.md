# 알라미(Alrami) — 프로젝트 방향 (Claude Code가 매 작업 전 읽는 문서)

> 이 파일은 "지금 무슨 작업"이 아니라 **"우리가 어디로 가는가"**를 알려준다.
> 작업 지시는 별도 프롬프트로 받되, 모든 작업은 아래 방향·원칙 안에서 한다.

---

## 무엇을 만드나 (한 문장)
**약정 만료를 대신 지켜보다가, 끝나기 직전에 갈아탈 곳과 이득을 찔러주는 앱.**
- 차별점: 경쟁사(모요·KTOA)가 하는 "비교"(사용자가 찾아와야 함)가 아니라,
  **"만료를 대신 감시해서 제때 미는 것(push)."** 그게 해자.
- 시작점: 약정 끝난 줄 모르고 지원금 없이 호구잡히는 손해를 막는다.
- 비유: 구독을 대신 감시하는 Rocket Money의 "약정 버전".

## 절대 원칙 (위반 시 그 작업은 틀린 것)
1. **계산은 전부 백엔드.** 프론트/AI는 표시·문구만. 절약액·혜택·실적·달성률을 프론트에서 계산하지 말 것.
2. **모바일 + 토스급 UX를 최우선.** 모든 화면은 핸드폰 기준(데스크톱 아님). 토스처럼 세련·단순·직관적으로 — 한 화면 한 목적, 최소 입력, 큰 탭 타깃, 스크롤 없이 핵심 노출. **복잡함·군더더기 제거가 기능 추가보다 우선.**
3. **추측 추천 금지.** 혜택/데이터 없는 카드·요금제를 추측해서 추천하지 말 것. 데이터 있을 때만.
4. **신뢰 표시.** 커미션/제휴는 숨기지 말고 표시. 추천 순서는 사용자 이득 기준(커미션순 아님). 숫자 부풀리지 말 것.
5. **git 인덱스 손상 주의.** 작업 전 git status 확인. 가짜 `frontend/` 항목·잘린 rename(`→ frontend/`·`→ Com`)·대량 deleted 보이면 인덱스 손상 → commit 금지.
   복구: `del .git\index.lock` → `del .git\index` → `git reset` → 재확인 `git ls-files | findstr /R "frontend/$"` 빈 출력이어야 정상.

## 제품 구조 (잠긴 결정)

### 1. 훅 — 절감 진단 (progressive, 최소 입력)
- 통신비 진단: 가입 직후 1분, **통신사 + 월 통신비**만 → "같은 데이터로 알뜰폰 가면 연 ○○만원 아껴요." (백엔드 quick-estimate)
- 카드 절감 티저(secondary): **총지출 1개** → "카드 잘 조합하면 **최대 연 ○○만원 더**" 헤드라인 → 원하면 슬라이더 2~3개(식비/쇼핑/기타)로 정밀화.
- 두 진단 모두 **"대략/최대" 추정으로 표기**(estimated). 단정·정밀 per-card 금액 금지. 약정 중이면 "위약금 있음" 동반.
- **훅→watchdog 다리**: "제일 이득인 타이밍은 약정 끝나는 날 → 약정일 알려주면 그때 알려줄게요."

### 2. 핵심 — 다품목 약정 watchdog
- 대상(지원금/위약금 걸린 것만): **휴대폰·인터넷·TV·정수기·비데·렌탈가전.** OTT·일반구독 제외.
- 제네릭 약정 모델 1개(category 필드만 다름). 타입별로 따로 만들지 말 것.
- 약정일: 빈 날짜칸 금지. "언제 가입?(대략) + 약정 몇 년?" → 백엔드가 종료일 계산(estimated).
  통신사/렌탈사 확인 경로 안내로 confirmed 승격. 틀려도 알림은 보냄.
- **약정 확인 도우미**: 약정일 모르면 회사 선택 → 고객센터 원탭 전화(tel:) + 앱 메뉴 경로 + 바로 아래 입력칸. 자동 조회는 불가(마이데이터에 약정 빠짐), 빠른 확인+즉시 입력이 최선. 데이터는 백엔드 테이블, 미확인 회사는 추측 금지·검색 fallback.
- 온보딩은 휴대폰부터, 나머지는 아이콘 그리드로 선택 추가.
- 만료 임박(D-90/30/7) → **카카오 알림톡 push** → 갈아탈 유리 조건 소개.

### 3. 수익 — 갈아타기 커미션(전 품목)
- 휴대폰·인터넷·정수기·렌탈 등 우리 통해 갈아탈 때의 커미션 = watchdog의 수익 층.
- **카드 발급 CPA는 지금 안 함**(잘라낸 카드추천을 되살리는 거라 off-mission). someday만.
- 파트너 계약 전: 정직 추천 + 전환(클릭) 추적으로 증명 후 협상.

### 4. 소비 개선 — 유지·강등·동결
- 이미 만든 것 버리지 않음. 단 **홈 첫 화면 금지**(별도 탭), **더 키우지 않음(freeze).**
- 카드 절감 티저(위 1번)가 이 영역의 가벼운 대체. 무거운 카드 내역 붙여넣기 의존 줄임.

### 리텐션
- **푸시 모델**: 대신 감시 → 안 열어도 됨. "입력하고 끝"이 정상. + 다품목/가족으로 이벤트 빈도↑ + 월1회 가벼운 리포트(가계부 아님).

## 안 하는 것 / 뺀 것
- 챗봇: 규칙엔진 후순위. LLM 미연결, 오픈챗(자유입력) 금지(비용).
- 일반 알람시계 UI: 제거(스케줄러 인프라는 약정 알림에 재활용).
- 카드 추천/카드 CPA, 가계부 확장: 지금 안 함.
- 문자 자동 파싱: **양쪽(iOS·안드) 수동 붙여넣기 기본.** iOS는 SMS API 없어 자동 불가(확정), 안드는 Google Play SMS 정책 장벽. 자동은 Play 승인 시에만 추가.

## 제약(검증된 사실)
- 통신 마이데이터(2025-03)에 **약정정보 빠짐**(통신3사 영업비밀) → 약정일 자동수집 불가, 수동이 최선.
- 비교/추천은 모요·KTOA가 이미 함(레드오션). 우리 차별점은 비교가 아니라 **타이밍 감시**.

## 작업 우선순위 (큰 흐름)
1. 홈 모바일 재구성(훅 진단 + 약정 watchdog 진입 + 붙여넣기 above-fold, 카드추천 큰 블록 축소).
2. 다품목 약정 watchdog: 제네릭 모델 + 온보딩(휴대폰) + 아이콘 추가.
3. 알림 파이프라인: 이메일 → 카톡 알림톡 전환(현재 스케줄러는 email만 호출, 카톡 함수는 미사용).
4. 훅 화면(통신비 진단 + 카드 절감 티저, 둘 다 progressive).
5. 수익화는 트래픽 증명 후. UI 미세 폴리시는 구조 확정 뒤 일괄.

## 보고 형식 (작업 끝낼 때마다)
수정파일 / 구현내용 / 미구현 / build결과 / git status / push결과 / 다음작업 + [현재 할 일][다음 할 일]
