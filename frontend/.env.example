# ── 프론트 환경변수 예시 (Vercel 환경변수에 입력) ──
# 배포된 백엔드 주소 + /api  (없으면 개발용 '/api' 프록시 사용)
VITE_API_URL=https://YOUR-BACKEND.cloudtype.app/api

# 카카오 로그인 (선택)
VITE_KAKAO_CLIENT_ID=
VITE_KAKAO_REDIRECT_URI=https://YOUR-FRONT.vercel.app/oauth/callback
